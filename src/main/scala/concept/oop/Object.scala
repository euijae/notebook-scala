package main.scala.concept.oop

/**
 * @note 1. An object has exactly one instance (i.e, singleton) so we can't instantiate it.
 *       2. An object in scala is like having a class in java that has only static methods.
 */
object Object  {}